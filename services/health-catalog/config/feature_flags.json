{
  "flags": {
    "use_health_catalog": true,

    "config_source": "health_catalog",
    "no_hardcode_policy": true,

    "mapping_source": "DB",
    "enable_patient_mapping_from_catalog": false,

    "enable_thresholds_from_catalog": true,
    "enable_alert_contract_from_catalog": true,
    "enable_mqtt_topics_from_catalog": true,
    "enable_service_endpoints_from_catalog": true,

    "enable_vitals_persistence": true,
    "enable_alert_persistence": true,
    "persistence_source": "mqtt",

    "enable_dashboard_mqtt": true,
    "enable_alert_notification": true,

    "enable_strict_schema_validation": true,
    "enable_debug_logging": true
  },

  "notes": {
    "wristband_patient_mapping": "Wristband→Patient mapping is dynamic and must come from DB (WRISTBAND_ASSIGNMENT). Health Catalog is for mostly static config (topics, service endpoints, default thresholds, alert rules).",
    "health_catalog_role": "Health Catalog = rules & configuration; DB = operational data & real system state.",
    "phase_2_policy": "All services must load configs from Health Catalog. No hardcoded topics, endpoints, thresholds, or alert rules are allowed."
  }
}
